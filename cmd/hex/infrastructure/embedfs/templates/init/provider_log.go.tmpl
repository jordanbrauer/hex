package provider

import (
	"github.com/jordanbrauer/hex/log/provider"
)

// Log applies the log level (and optional caller/timestamp toggles)
// from config to hex/log. Customise the defaults here.
func Log() *provider.Provider {
	return &provider.Provider{
		Namespace: "log",
	}
}
