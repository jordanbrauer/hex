// Package {{.Package}} is the {{.Name}} domain — entities, value objects,
// and business rules. It has no dependencies on infrastructure; use
// Repository (repository.go) as the port to the outside world.
package {{.Package}}

import "time"

// {{.Name}} is the aggregate root.
type {{.Name}} struct {
	ID        string
	CreatedAt time.Time
	UpdatedAt time.Time
}

// New constructs a new {{.Name}}. Add required fields as parameters.
func New(id string) *{{.Name}} {
	now := time.Now().UTC()

	return &{{.Name}}{
		ID:        id,
		CreatedAt: now,
		UpdatedAt: now,
	}
}
