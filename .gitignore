# Build / test artefacts
*.test
*.out
coverage.out
coverage.txt
coverage.html
/hex
/dist/
# Rendered manpages (roff). Source of truth is docs/man/*.md; the roff is
# regenerated by `just man` locally and at release time. pandoc's version
# affects the exact roff, so it is not committed.
/man/
# Compiled example binaries (produced by `go build` inside each example)
examples/ai-lua/ai-lua
examples/swapi/swapi

# Environment
.env
.env.local
!.env.dist

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Tooling caches
/tmp/
.golangci-lint-cache/
